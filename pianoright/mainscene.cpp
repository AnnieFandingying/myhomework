#include "mainscene.h"
#include"change.h"
#include<QIcon>
mainsene::mainsene(QWidget *parent)
    : QWidget(parent)
{

}

mainsene::~mainsene() {}
void mainsene::initscene()
{
    //设置窗口初始尺寸
    //setFixedSize(WIDTH,HEIGHT);
    //设置标题
    //setWindowTitle(TITLE);
    //设置图标
    //setWindowIcon(QIcon(":/res/tubiao.ico"));
}
